# Workforce Plugin v1

Workforce Planning owns members, contracts, day status, availability,
requirements, coverage and audit history. It publishes neutral Human Resource,
Resource Availability and Time Window contracts. It never creates operational
Assignments and has no dependency on an Adapter.

Enable it with `WORKFORCE_PLUGIN_ENABLED=true`. The production-safe default is
disabled.
